<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DataReflix" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/proyecto_light</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>

    </style>

    <style name="Theme.DataReflix" parent="Base.Theme.DataReflix" />

    <style name="Theme.DataReflix.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DataReflix.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.DataReflix.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>